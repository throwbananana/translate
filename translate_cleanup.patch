diff --git a/README.md b/README.md
index 151ed66..a355fe2 100644
--- a/README.md
+++ b/README.md
@@ -1,42 +1,69 @@
-# translate patch series (mailbox-style)
+# 书籍翻译工具 (Book Translator)
 
-This directory contains mailbox-style wrappers around the earlier unified diffs.
-They are meant to be easier to review and to convert into real commits later.
+一个桌面端书籍翻译工具，支持 PDF、TXT、EPUB、DOCX、Markdown 等常见格式，并可接入 Gemini、OpenAI、Claude、DeepSeek 以及兼容 OpenAI API 的自定义服务。
 
-## What these files are
+## 当前状态
 
-- `0000-cover-letter.patch`: series overview
-- `0001`..`0004`: one patch-message per logical change
+这个仓库当前更适合作为源码仓库直接运行，而不是“patch series”目录。
+本 README 旨在提供可落地的安装与配置说明。
 
-## Important limitation
+## 功能概览
 
-These patches were generated from prepared diff files, not exported from a real
-Git commit history. The `From <sha>` values are synthetic content hashes used to
-make the files look and behave more like `git format-patch` output.
+- 多格式导入：PDF / TXT / EPUB / DOCX / Markdown
+- 多翻译后端：Gemini / OpenAI / Claude / DeepSeek / 自定义 OpenAI 兼容接口
+- 翻译记忆库与术语表
+- 批量任务与断点续传
+- PDF OCR 扫描件支持（依赖 pdf2image + Tesseract）
 
-## Apply options
+## 安装
 
-### If you only want the file changes
+### 1) 安装 Python 依赖
 
 ```bash
-git apply 0001-secure-config-and-ignore-runtime-data.patch
+py -m pip install -r requirements.txt
 ```
 
-Use `git apply --check <patch>` first if you want to verify applicability.
+### 2) 安装 OCR 所需系统依赖（可选，但 PDF 扫描件 OCR 需要）
 
-### If you want to turn them into commits later
+- 安装 **Poppler**，确保 `pdftoppm` 可执行文件在 PATH 中
+- 安装 **Tesseract OCR**，确保 `tesseract` 可执行文件在 PATH 中
 
-These mailbox-style files are best treated as review artifacts first. The most
-reliable path is:
+如果你不需要 OCR，可以只安装 Python 依赖并关闭/跳过扫描件识别功能。
 
-1. apply the original plain diffs or these mailbox diffs on a topic branch
-2. inspect and test the result
-3. create real commits with `git commit`
-4. export them again with `git format-patch`
+## 配置
 
-## Suggested order
+项目运行时会优先把配置写到用户目录，而不是源码目录。
 
-1. `0001` security and ignore rules
-2. `0002` runtime path and translation-memory relocation
-3. `0003` DOCX stabilization
-4. `0004` parallel batch hardening
+推荐使用环境变量提供敏感信息：
+
+- `GEMINI_API_KEY`
+- `OPENAI_API_KEY`
+- `ANTHROPIC_API_KEY`
+- `DEEPSEEK_API_KEY`
+- `CUSTOM_API_KEY`
+- `LM_STUDIO_API_KEY`
+
+如需手动创建配置文件，请复制模板：
+
+```bash
+copy translator_config.example.json translator_config.json
+```
+
+> 建议：API Key 优先使用环境变量；本地配置文件仅适合保存非敏感默认项。
+
+## 快速启动
+
+```bash
+py book_translator_gui.pyw
+```
+
+## 说明
+
+- 仓库不再建议提交 `translator_config.json`、`translation_memory.db` 等运行态文件
+- `translator_config.example.json` 仅作为模板，不应包含真实凭据
+- Claude 默认模型已更新为 `claude-haiku-4-5-20251001`
+
+## 开发建议
+
+- 提交前先运行基础语法检查与核心测试
+- 不要把本地数据库、缓存和配置备份目录提交到仓库
diff --git a/book_translator_gui.pyw b/book_translator_gui.pyw
index 75f0a26..953d0f3 100644
--- a/book_translator_gui.pyw
+++ b/book_translator_gui.pyw
@@ -10,7 +10,10 @@
 v2.3 新增：PDF OCR扫描件支持 (pdfplumber+pdf2image)、可视化术语表编辑、章节目录导航、批量任务断点续传
 
 依赖库:
-py -m pip install PyPDF2 pdfplumber pdf2image ebooklib beautifulsoup4 google-generativeai openai requests python-docx
+py -m pip install -r requirements.txt
+系统依赖（OCR 可选）:
+- 安装 Poppler，并确保 pdftoppm 在 PATH 中
+- 安装 Tesseract OCR，并确保 tesseract 在 PATH 中
 """
 
 import tkinter as tk
@@ -97,7 +100,7 @@ DEFAULT_LM_STUDIO_CONFIG = {
 DEFAULT_API_CONFIGS = {
     'gemini': {'api_key': '', 'model': 'gemini-2.5-flash'},
     'openai': {'api_key': '', 'model': 'gpt-3.5-turbo', 'base_url': ''},
-    'claude': {'api_key': '', 'model': 'claude-3-haiku-20240307'},
+    'claude': {'api_key': '', 'model': 'claude-haiku-4-5-20251001'},
     'deepseek': {'api_key': '', 'model': 'deepseek-chat', 'base_url': 'https://api.deepseek.com/v1'},
     'custom': {'api_key': '', 'model': '', 'base_url': ''},
     'lm_studio': deepcopy(DEFAULT_LM_STUDIO_CONFIG)
@@ -1924,7 +1927,7 @@ class BookTranslatorGUI:
         help_text = {
             'gemini': "请在 Google AI Studio 获取 API Key\n模型示例: gemini-2.5-flash, gemini-2.5-pro",
             'openai': "请在 OpenAI 获取 API Key\n模型示例: gpt-3.5-turbo, gpt-4",
-            'claude': "请在 Anthropic Console 获取 API Key\n模型示例: claude-3-haiku-20240307",
+            'claude': "请在 Anthropic Console 获取 API Key\n模型示例: claude-haiku-4-5-20251001",
             'deepseek': "请在 DeepSeek 开放平台获取 API Key\n模型示例: deepseek-chat",
             'custom': (
                 "输入兼容OpenAI API格式的自定义服务\n"
@@ -5049,7 +5052,8 @@ def main():
         for lib in missing_libs:
             print(f"  - {lib}")
         print("\n安装命令:")
-        print("py -m pip install PyPDF2 ebooklib beautifulsoup4 google-generativeai openai requests")
+        print("py -m pip install -r requirements.txt")
+        print("如需 OCR，再额外安装 Poppler 与 Tesseract 并加入 PATH")
         print("=" * 60)
         print()
 
diff --git a/config_manager.py b/config_manager.py
index 9492c17..c71a447 100644
--- a/config_manager.py
+++ b/config_manager.py
@@ -8,7 +8,7 @@
 - 配置文件读写
 - 自动备份和恢复
 - 版本迁移
-- API Key 加密存储（可选）
+- API Key 轻量编码存储（可选）
 - 默认值管理
 """
 
@@ -27,6 +27,12 @@ from app_paths import get_app_dir, get_backup_dir
 # 配置版本
 CONFIG_VERSION = "2.3.1"
 
+DEFAULT_OPENAI_MODEL = "gpt-3.5-turbo"
+DEFAULT_CLAUDE_MODEL = "claude-haiku-4-5-20251001"
+DEPRECATED_MODEL_REPLACEMENTS = {
+    'claude-3-haiku-20240307': DEFAULT_CLAUDE_MODEL,
+}
+
 # 默认配置
 DEFAULT_CONFIG = {
     'version': CONFIG_VERSION,
@@ -45,13 +51,13 @@ DEFAULT_CONFIG = {
         },
         'openai': {
             'api_key': '',
-            'model': 'gpt-3.5-turbo',
+            'model': DEFAULT_OPENAI_MODEL,
             'base_url': '',
             'temperature': 0.2
         },
         'claude': {
             'api_key': '',
-            'model': 'claude-3-haiku-20240307',
+            'model': DEFAULT_CLAUDE_MODEL,
             'temperature': 0.2
         },
         'deepseek': {
@@ -128,9 +134,6 @@ class ConfigManager:
         # 当前配置
         self._config: Dict = deepcopy(DEFAULT_CONFIG)
 
-        # 加密密钥（简单混淆，非安全加密）
-        self._key = "book_translator_2024"
-
         self._migrate_legacy_config()
 
         # 自动加载配置
@@ -156,6 +159,19 @@ class ConfigManager:
         except Exception as e:
             print(f"⚠️ 迁移旧配置失败: {e}")
 
+    def _normalize_api_models(self, config: Dict) -> Dict:
+        """归一化已弃用的默认模型，避免新安装就踩到即将退役的模型。"""
+        config = deepcopy(config)
+
+        for provider in config.get('api_configs', {}).values():
+            if not isinstance(provider, dict):
+                continue
+            model = provider.get('model')
+            if model in DEPRECATED_MODEL_REPLACEMENTS:
+                provider['model'] = DEPRECATED_MODEL_REPLACEMENTS[model]
+
+        return config
+
     def _apply_env_overrides(self, config: Dict) -> Dict:
         """允许通过环境变量覆盖 API Key，避免把敏感信息写入文件。"""
         env_map = {
@@ -327,7 +343,7 @@ class ConfigManager:
         """
         try:
             if not self.config_path.exists():
-                self._config = self._apply_env_overrides(deepcopy(DEFAULT_CONFIG))
+                self._config = self._apply_env_overrides(self._normalize_api_models(deepcopy(DEFAULT_CONFIG)))
                 print("ℹ️ 配置文件不存在，使用默认配置")
                 return False
 
@@ -340,6 +356,8 @@ class ConfigManager:
                 loaded = self._migrate_config(loaded, loaded_version)
                 print(f"ℹ️ 配置已从 v{loaded_version} 迁移到 v{CONFIG_VERSION}")
 
+            loaded = self._normalize_api_models(loaded)
+
             # 合并默认值
             self._config = self._apply_env_overrides(self._merge_with_defaults(loaded))
 
diff --git a/requirements.txt b/requirements.txt
index 6e2f11a..b5d32d7 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -2,6 +2,7 @@
 # 核心依赖
 google-generativeai>=0.3.0
 openai>=1.0.0
+anthropic
 requests>=2.31.0
 
 # 文件格式支持
diff --git a/translator_config.json b/translator_config.example.json
similarity index 91%
rename from translator_config.json
rename to translator_config.example.json
index ba91fff..da7caea 100644
--- a/translator_config.json
+++ b/translator_config.example.json
@@ -12,7 +12,7 @@
     },
     "claude": {
       "api_key": "",
-      "model": "claude-3-haiku-20240307"
+      "model": "claude-haiku-4-5-20251001"
     },
     "deepseek": {
       "api_key": "",
@@ -31,8 +31,8 @@
     }
   },
   "custom_local_models": {},
-  "target_language": "英文",
+  "target_language": "中文",
   "selected_translation_api": "Gemini API",
   "selected_analysis_api": "Gemini API",
   "selected_retry_api": "本地 LM Studio"
-}
\ No newline at end of file
+}
