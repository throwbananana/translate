From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Patch Generator <generated@example.invalid>
Date: Sat, 28 Mar 2026 12:00:00 +0000
Subject: [PATCH 0/4] translate: safety, runtime-path, DOCX, and batch hardening

This series packages the earlier unified diffs into mailbox-style patches that
are easier to review, archive, and later turn into real commits.

The four patches are ordered by rollback risk:

  1. secure config handling and ignore runtime artifacts
  2. move runtime files and translation memory into a user directory
  3. stabilize the DOCX handler to a conservative export path
  4. harden GUI-side parallel batch execution

These files are generated wrappers around the already prepared diffs, so the
"From" object ids are synthetic placeholders rather than real commit hashes.
They are intended for review and for later conversion into a real git history.

Patch summary:

  0001 translate: secure config and ignore runtime data
  0002 translate: move runtime data and TM storage to user dir
  0003 translate: stabilize DOCX handler export path
  0004 translate: harden batch parallel translation handling

-- 
2.45.0
